import { Link } from "react-router-dom"

const Landing = () => {
    return(
        <div>
            <Link to="/home">
            <button>ENTER</button>
            </Link>
        </div>
    )
}

export default Landing